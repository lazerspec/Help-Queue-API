pipeline {
    agent any

    stages {
        stage('Docker Build') {
            steps {
               sh "echo Building the application..."

            }
        }
        stage('Deploy to Dockerhub') {
            steps {
         sh "echo in deploy step"

            }
        }
    }
}